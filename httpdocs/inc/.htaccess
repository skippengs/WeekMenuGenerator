# Deze map bevat geen pagina's, alleen code. Nooit rechtstreeks opvraagbaar.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
